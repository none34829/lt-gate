# Core dependencies
torch>=2.0.0
types-torch>=0.1.1  # Type stubs for PyTorch
h5py>=3.8.0
numpy>=1.24.0
pandas>=2.0.0
pandas-stubs>=2.0.0  # Type stubs for pandas
matplotlib>=3.7.0
seaborn>=0.13.0  # Required for advanced violin plots
scipy>=1.10.0
pingouin>=0.5.4  # For robust effect-size & Bayesian tests
jupyter>=1.0.0
tqdm>=4.65.0

# Neuromorphic hardware - Note: Some packages may have Python version compatibility issues
# with Python 3.11. Consider using Python 3.10 if these fail to install.
akida>=2.15.0
cnn2snn>=2.15.0
quantizeml>=0.18.1
akida-models>=1.7.0

# Testing and development
pytest>=7.3.1
mypy>=1.17.1

# Note: types-h5py is not available for Python 3.11
# Consider using Python 3.10 if type stubs are needed
